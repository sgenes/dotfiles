# ls colors
autoload -U colors && colors

# Enable ls colors

#setopt no_beep
setopt auto_cd
setopt multios
setopt cdablevars

# if [[ x$WINDOW != x ]]
# then
    # SCREEN_NO="%B$WINDOW%b "
# else
    # SCREEN_NO=""
# fi

# Setup the prompt with pretty colors
